// Index.jsx
// This is the main landing page of the app. It simply renders the CalendarApp component.

import CalendarApp from '@/components/CalendarApp';

// Index component renders the main calendar application
const Index = () => {
  return <CalendarApp />;
};

export default Index; 